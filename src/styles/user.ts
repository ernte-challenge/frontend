import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles({
  registerGrid: {
    minHeight: '100vh'
  },
  registerButton: {
    margin: '1rem',
  },
});

export default styles;
